"use client";
import { Header } from "@/components";

const Home = () => (
    <main className="flex min-h-screen flex-col items-center justify-between">
        <Header/>
    </main>
);

export default Home;
